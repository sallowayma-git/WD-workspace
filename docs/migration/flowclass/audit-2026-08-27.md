# Flowclass 融合并发审计报告（2026-08-27）

> 审计对象：分支 `codex/flowclass-fusion`（工作树全部融合改动未提交：185 删除 / 56 修改 / 27 新增路径）
> 审计基准：`DocsHarness/04_Flowclass到WD_助教工作台二开融合任务书_v1.0.md`（§19.9 附录 2026-08-22 为最新收口）
> 审计方式：6 条审计线并发子代理（只读取证）+ 主代理实时门禁复跑；全部结论附 repo 相对 file:line 证据
> 状态标记：✅VERIFIED / ⚠️PARTIAL / ❌MISSING / 🚫N.A.

---

## 1. 审计概况

### 1.1 范围与分工

| 审计线 | 范围 | 状态 |
|---|---|---|
| A 验收清单与阶段门禁 | 04 §16 F0~F9、§19 ACC-001~074、acceptance.md、根 planning 漂移 | ✅ 完成 |
| B Calendar 移植与样式兼容层 | 04 §5、§6、§15、§18.1 | ✅ 完成 |
| C 调度域算法与统一命令 | 04 §8 AVL、§9 DLY、§11 INT-CAL、§12 RSC、§13.2 | ✅ 完成 |
| D 矩阵融合与工作台 | 04 §7 MAT、MATRIX-GATE、§18.2 | ⏳ 运行中（本文档待补） |
| E 本地数据层与清理 | 04 §13 LOC、§14 CLEAN、§20 R10、§19.1 | ✅ 完成 |
| F 模型边界与 WD 模块保留 | 04 §1.2/§1.3、§4.2、§10、§20 R1~R9、§24 DoD | ✅ 完成（主代理接手） |

### 1.2 实时门禁证据（主代理 2026-08-27 实测，非文档声称）

- `pnpm check` 全绿：`local-runtime-ok (59 reachable source modules, no HTTP transport)`；lint + typecheck 通过；**Web 14 个测试文件 / 61 个用例全部通过**；`cargo fmt --check` + `cargo clippy -D warnings` 通过。
- `pnpm format:check` 通过。
- CI（.github/workflows/ci.yml）与 README 已对齐本地桌面形态，无 Java/PostgreSQL/auth 残留条目。
- 上述实测与 04 §19.9 声称的自动化证据一致。

### 1.3 审计口径

- 旧在线架构（auth/RBAC/租户/PostgreSQL/apps/api）已按 04 §14 有意 DROP，其缺失不计为缺陷。
- 04 §19.9 附录（2026-08-22）优先级高于正文历史勾选与 DocsHarness/01~03 中与之冲突的在线架构要求；但业务不变量（只有完成推进 Track；顺延保留历史且双向链；命令幂等；锁定任务不自动移动）仍有效。

---

## 2. 执行摘要

**总判定：无 CRITICAL 数据损坏或虚假验收问题；核心闭环（持久化、模板/Track 幂等推进、顺延 lineage、三视图同源、locked 三层防线）有直接代码与测试证据。主要风险集中在门禁记录层与测试覆盖缺口，而非产品代码。**

按优先级的行动清单：

| # | 级别 | 事项 | 来源 |
|---|---|---|---|
| 1 | MAJOR | 根目录 task_plan.md / findings.md / progress.md 仍把已废止的在线架构当"硬门禁"，会误导后续会话/代理做反向重构 | 审计线 A |
| 2 | MAJOR | DLY 必测场景 T02/T08 无用例、T05/T06/T09 仅间接覆盖——F5"通过 DLY-T01~T10"门禁实质未满足 | 审计线 C |
| 3 | MAJOR | `previewCarryForward`（DLY-019 / §9.3 / §18.4）全库未实现 | 审计线 C/F |
| 4 | MAJOR | F0~F5 阶段门禁缺逐项收口说明；F0-004 WD 三页截图永久缺失；screenshots/ 未纳入版本控制 | 审计线 A |
| 5 | MAJOR | 顺延幂等存在"existing target 日期强匹配"缺口（DLY-018），domain 纯函数不应依赖调用方纪律 | 审计线 C |
| 6 | MAJOR | 同日排序缺 priority/star 维度（INT-CAL-009），规则两头都不成立 | 审计线 C |
| 7 | MAJOR | 任务详情五处占位"待实现"；TaskCard 顺延 Tag 无来源日期 tooltip（DLY-022/INT-CAL-003） | 审计线 C |
| 8 | MINOR | `execution-parity.md` 未标注被产品决策取代的范围；改期直生效与 expectedVersion 仅限 delete 的口径漂移未补记 | 审计线 A/C/F |
| 9 | MINOR | Schedule 页缺"学习日规则入口"（AVL-013）；"复制上周"被空态门禁（AVL-011） | 审计线 C |
| 10 | MINOR | 全部融合产物（含 185 删除、docs/migration、vendor）未提交，证据文件无版本保护——vendor 与 docs/migration 整目录 untracked，SHA-256 来源链只存在于工作树 | 审计线 A/B |
| 11 | MINOR | local-runtime 门禁理论绕过面（XHR 族/fetch 别名/计算型动态 import）；verify-desktop 表数阈值 <10 应为 !==13 | 审计线 E |
| 12 | MINOR | 注释/描述残留与死代码：browserSqliteStorage VITE_DATA_MODE 注释、Cargo.toml "API 提供"措辞、TaskCalendarEvent 死类型、compat.css 双重导入、provenance 缺第三方包小节与弃用表补全 | 审计线 E/B |

---

## 3. 审计线 A：验收清单与阶段门禁一致性

### 3.1 覆盖完整性

acceptance.md（docs/migration/flowclass/acceptance.md:3）声称 46 项，与 §19.1~19.8 清单逐一比对：001-005(5)、010-015(6)、020-024(5)、030-033(4)、040-045(6)、050-056(7)、060-067(8)、070-074(5) = 46 项，**全部出现且无多余编号**。另含 4 条"产品调整"行。**GAP 0 成立**（就编号覆盖而言）。

### 3.2 核心闭环项抽查判定（全表见 acceptance.md，此处为核心项证据）

| ACC | 判定 | 证据 |
|---|---|---|
| ACC-001 启动不要求登录 | ⚠️PARTIAL | 代码证据充分：apps/web/src/main.tsx:1-49 无 auth provider；App.tsx:15-38 无登录路由；features/auth、lib/api/http.ts 已不存在；check-local-runtime.mjs:8-63 禁 auth/fetch 且入 CI（ci.yml:25）。缺 exe 实际启动人工复核记录 |
| ACC-002 不要求 PostgreSQL | ✅ | apps/desktop/src-tauri/src/lib.rs:22 预载 sqlite:assistant.db + migration；runtime.test.ts:21-41 断言不调 fetch；sqliteLocalDataAdapter.test.ts:33-95 全部跑在真实 migration SQL 上 |
| ACC-003 不要求 Java/Spring | ✅ | apps/api 已不存在；暂存区 185 个 D；package.json:15-27 无 api/gradle；CI 无 Java job；docs/reference/retired-server/README.md:21-25 说明取回方式 |
| ACC-004 核心数据写 SQLite | ✅ | sqliteLocalDataAdapter.ts（2942 行，13 张表）；local_database.rs:52-90 Rust 单事务（含回滚测试 98-147）；adapter 测试 :105-409 |
| ACC-005 重启数据仍在 | ✅ | local_database.rs:26-32 app_config_dir + create_if_missing + WAL；测试 :1057-1088 关库重开可读 |
| ACC-010~015 学生 | ✅ | StudentListPage.test.tsx:78-155（搜索进 adapter 查询）；adapter :458 SQL LIKE；三入口路由 App.tsx:18-29；WeeklyPatternEditor/WeekPlanEditor + saveWeeklyPattern(:606)/saveWeekPlan(:681)；override 投影测试 :902-985 |
| ACC-020~024 模板/Track | ✅ | TemplateDetailPage.tsx:322,334 保存重排 ordinal；DB UNIQUE(template_version_id, ordinal)（migration:116）；MountTrackModal.tsx:5,95 → trackApi.mountTrack；纯函数幂等短路+连续前缀 taskTransitions.ts:48-50,66-71；adapter 单事务 :1718-1785；重复完成→ordinal 2、仅 1 条 PENDING（测试 :246-267）；改期/顺延不写 track（:1862-1880；测试 :277-286,316-321） |
| ACC-030~033 Today | ✅ | TodayPage.tsx:91 useBusinessDate()；:557-667 每生一卡（测试 :100-108）；:108-147 乐观更新+invalidateTaskViews（测试 :111-152）；InlineTaskComposer 挂 :660-664（测试 :154-210） |
| ACC-040~045 Matrix | ✅ | StudentWorkbenchPage.test.tsx:104-146（1+7 列表头、sticky left:0、minWidth 1300）；Compact 2/Expanded 5（:58-62）；同格多卡（测试 :148+）；useVirtualizer（shell :14,44-46；渲染行数 < 60 测试 :50-76） |
| ACC-050~056 Calendar | ✅（除 053 ⚠️） | scheduleWindow day/week/month 三分支（adapter :151-169）+ 页面测试 :95-136,203-258；ACC-053 命令链全 AUTO（StudentSchedulePage.tsx:567-603 → adapter :1845-1888）但指针手势未人工复核；locked 三层防线（useDraggable disabled :1342-1345 + canMoveCalendarEvent :574-582 + SQL AND locked=0 :1329,1412,1875）；无课程残留（测试 :139 逐视图扫描） |
| ACC-060~067 顺延 | ✅ | carryForwardTask（taskTransitions.ts:179-253）；右键菜单 TaskContextMenu.tsx:103-105；跳过不可学习日 90 天上限（availability.ts:90,94）；override 优先（:53-58）；源置 CARRIED_OVER（:243-249）+ deleteTask 拒删历史（adapter :2027-2028）；双向 lineage 同事务回填（migration:169-170；测试 :296-314 双向断言）；顺延后重读 track ordinal 不变（测试 :316-321，注释引用 ACC-065）；复用既有 target + uq_task_carry_target（migration:192-194）；日结重跑 scanned=0（测试 :501-506）；首页执行日结（TodayPage.tsx:371-394,480-489） |
| ACC-070~074 三视图一致 | ✅（除 072 部分 ⚠️） | invalidateTaskViews 统一失效四视图（taskActions.ts:17-33；测试 :6-40）；三视图同源同一 tasksBetween 查询（adapter :1584,1627,1681,2693-2699；三投影一致测试 :372-398）；history 行禁勾选（scheduleApi.ts:20-23 + TaskCard.tsx:114-155；测试 :260-295） |
| 产品调整×4 | ✅ | 编号留空自动生成（StudentListPage.tsx:197 + adapter :487-497）；全仓无批量 UI；跨学生拖拽（workbenchDrag.ts:28-50 + rescheduleTask 跨生脱离 Track taskTransitions.ts:152-176 + SQL CASE :1862-1865 + 测试 :716,768）；顺延目标可删清 lineage（adapter :2033-2051；测试 :509-637） |

### 3.3 汇总数字核对

- "46 项" ✅；"14 个测试文件" ✅（实测 14）；"61 个用例" ✅（59 个 it 声明，其中 1 个 it.each 展开 3 例，59−1+3=61）；本地实时复测一致（§1.2）。
- **"AUTO 42 / MANUAL 4" 存在 ±1 记账冲突**：按表格逐行数是 AUTO 43 / MANUAL 3（ACC-072 表内标 "AUTO（命令）" 但汇总计入 MANUAL 4）；且 §19.9 的 MANUAL 口径（启动/拖拽手感/滚动手感/提交策略）与 acceptance.md 的 MANUAL 4（ACC-001/003/053/072）枚举不一致。需统一口径。
- MANUAL 4 项基本确实必须人工（exe 启动、无 java/postgres 进程只能真机确认；指针拖拽 jsdom 无法模拟）。

### 3.4 Phase F0~F9 门禁核对

| Phase | 状态 | 说明 |
|---|---|---|
| F0-001 分支 | ✅ | codex/flowclass-fusion；baseline.md:8 |
| F0-002 基线测试结果 | ⚠️ | baseline.md:14-21 有结果表但无原始输出存档 |
| F0-003 最小回归列表 | ✅(等效) | execution-parity.md 即清单；DLY-T01~T10 在测试有部分着落但未引用编号（可追溯性弱） |
| F0-004 WD 三页截图 | ❌ | baseline.md:29-35 自认无可信截图（Docker 缺失跑不起旧栈）。**已不可补** |
| F0-005 Flow 截图 | ⚠️ | 仅 Calendar PoC 两张（screenshots/ 内确认存在）；AttendanceSheet/Availability 无截图；目录未入 git |
| F0-006 provenance.md | ✅ | 含 ZIP SHA-256、逐文件 Source SHA-256、COPY/ADAPT/DROP 决策表 |
| F1 样式兼容层 | ⚠️ | 交付物在且达成 R8（scoped、无 preflight）；但 STYLE-001~010 无逐项记录，实现走手写 scoped CSS 而非 STYLE-001 原文的 Tailwind 方案，deviation 未记录 |
| F2 Calendar PoC | ✅ | CalendarAdoptionDecision.md 与代码一致（DROP 文件确不在盘上） |
| F3 Matrix 融合 | ⚠️ | 交付物在；过渡期门禁（现有 WD 后端数据可完整操作）无记录，后端已删无法回溯 |
| F4 Availability 融合 | ⚠️ | 交付物在（已接入 StudentProfilePage:409-413）；"改变后端 next available"门禁对象已删 |
| F5 Delay 提纯 | ⚠️ | TS 算法+测试在；与 Java 行为对照无记录（R7 只有单侧证据）；且 DLY-T02/T05/T06/T08/T09 缺直接用例（见审计线 C） |
| F6 统一命令 | ✅ | complete/reopen/reschedule/carry 收敛于 taskActions.ts:8-15 单一 facade；canMoveCalendarEvent 唯一移动守卫 |
| F7 SQLite 数据层 | ✅ | 04:1236 已勾；data/local/ + migration + pnpm check 实证 |
| F8 清理在线架构 | ✅ | 04:1243 已勾；CLEAN-004/007/008/012/013/014 逐一核实；check-local-runtime + CI 门禁在位 |
| F9 完整验收 | ⚠️未收口 | AUTO 部分可信；MANUAL 复核步骤 1-5（acceptance.md:123-134）无执行记录 |

### 3.5 根目录 planning 文件漂移（MAJOR-1）

task_plan.md / findings.md / progress.md 自 2026-08-18 起未再实质更新；三份文件中 flowclass/SQLite/fusion 出现次数为 0。失效陈述举例：

| 位置 | 失效陈述 | 现实 |
|---|---|---|
| task_plan.md Decisions 表 | "中心 PostgreSQL 身份/RBAC/持久会话/租户隔离均为硬门禁" | 在线架构已整体 DROP |
| task_plan.md Guardrails | "不宣称 Phase 0 完成，直到数据库身份/RBAC/租户…真实 PostgreSQL 证据" | Phase 0 概念已被 F0~F9 取代；PostgreSQL 永不回归 |
| task_plan.md Phase 0~3 勾选 | PostgreSQL 18/Flyway [x]、登录/刷新 [x]、FTS/trigram [x] | 这些代码已删除；勾选描述一套不存在的系统 |
| findings.md 全文 | RBAC 缺口、租户隔离、AuthSessionStore、Testcontainers、"Backend: 20 unit tests pass" | 全部指向已删除的 Spring/PostgreSQL 栈 |
| progress.md 末段 | "PRD AC-001~015: 15/15 fully satisfied"、"ArchitectureTest: PASS" | 对现仓库为假陈述（无后端可测）；融合工作（08-19~08-22）未记录 |

**影响**：按 planning-with-files 恢复会话的后续代理会从"PostgreSQL/RBAC 硬门禁"出发，把本地 SQLite 化当待修复偏差，甚至尝试重建 Spring 门禁——与 04 §19.9 直接冲突。

**建议**：三份文件顶部加"废止声明"（指向 04 §1/§19.9、ADR-002、README），Phase/Final Acceptance 改写为 F0~F9 + ACC 矩阵口径；保留历史段落但标注"已废止 (2026-08-20)"。

### 3.6 审计线 A 问题清单

- **MAJOR-1** 根目录 planning 三件套整体漂移（见 3.5）。
- **MAJOR-2** F0 视觉基线缺失且不可补：F0-004 无任何 WD 页面截图；F0-005 只覆盖 Calendar；F0~F5 无逐项收口说明（§16 复选框全空与"F7/F8 已完成"并置，读者无从判断 F0~F5 是否算完成）。建议在 04 §16 或 baseline.md 补收口说明（完成/等效完成/有意跳过+理由），F0-004 标"已跳过，不可补"；screenshots/ 纳入版本控制。
- **MAJOR-3（记账）** AUTO 42/43 与 MANUAL 3/4 口径冲突；§19.9 与 acceptance.md 的 MANUAL 枚举不一致。建议统一（ACC-072 行改标 "MANUAL（命令 AUTO）"，或汇总改 43/3 + 复核清单项）。
- **MINOR-1** execution-parity.md:26-28 仍以现行契约口吻写 "Only PENDING and BLOCKED can be rescheduled / device-policy conflicts remain distinct errors"，但 2026-08-20 起改期直生效、非学习日照做并记 override 原因、COMPLETED 也可移动（taskTransitions.ts:122-131；测试 :230）。建议文件头加 supersession 注记，保留仍有效部分（事务边界、幂等、locked/历史拒绝）。
- **MINOR-2** parity 门禁只有单侧证据（baseline.md:15 一行声明，无输出存档）；Java 已删无法补跑。建议如实记录"对照方式=契约逐条评审+基线日 Java 测试通过声明"。
- **MINOR-3** Rust 事务回滚测试（local_database.rs:98-147）不在任何门禁内运行：check:desktop 只跑 fmt/clippy，CI desktop job 无 cargo test。建议补入。
- **MINOR-4** 全部融合产物未提交；screenshots/ 未跟踪，一次 git clean 即永久丢失。建议尽快分批提交（至少先提交 docs/migration 与 vendor/provenance 证据类文件）。
- **MINOR-5** §19.9 与 acceptance.md 对日结口径措辞差异（04:1406 "批量结果可见" vs acceptance.md:98 "不渲染运行日志"）。实现两头都顾到（汇总+明细卡），加半句说明即可。
- **MINOR-6** "59 个可达源码模块"等脚本输出数字无存档。建议把最近一次 pnpm check 输出存入 docs/migration/flowclass/ 作为证据快照（本次实测输出可作底稿）。

---

## 4. 审计线 C：调度域算法与统一命令

### 4.1 AVL-001~017（学习日编辑器）

| 编号 | 状态 | 证据 |
|---|---|---|
| AVL-001 WeekdayList 视觉结构 | ⚠️ | AvailabilityDayRow.tsx:52-102 行式布局+availability.css:6-27 grid；多一列设备策略 Select，非原样复刻 |
| AVL-002 删除 time picker | ✅ | 全文无 TimePicker，仅分钟 InputNumber |
| AVL-003 Switch+InputNumber | ✅ | AvailabilityDayRow.tsx:58-62,67-77（min0 max1440 step15） |
| AVL-004 关闭日=0分钟 | ✅ | :36-41；测试 AvailabilityDayRow.test.tsx:20-44 |
| AVL-005 不偷偷设 60 | ✅ | :29-49 lastPositiveMinutes 回填；无历史提示"请填写"（:98-100）；测试 :46-62 |
| AVL-006 DateOverride 模式 | ⚠️ | 改造成整周替换式（WeekPlanEditor.tsx:138-215；adapter :735-738 DELETE+INSERT），无 Flow 单条增删模式 |
| AVL-007 override 字段 | ⚠️ | domain 恰为 date/available/availableMinutes(+devicePolicy)（availability.ts:11-16）；持久层多 devicePolicyOverride/sourceType/note，功能超集 |
| AVL-008/009/010 DROP 项 | ✅ | 无 ApplyToClass/多 profile；每生仅 ACTIVE 常规周一条（adapter :573-579,621-645） |
| AVL-011 复制上周仅 override 层 | ⚠️ | PREVIOUS_WEEK 只写 student_date_override（:704-714）；但按钮仅周计划 404 空态出现（WeekPlanEditor.tsx:114-135），已有计划无法再触发 |
| AVL-012 Profile 摘要 | ⚠️ | StudentProfilePage.tsx:404-414 有学科/常规周/周计划卡片，无独立精简摘要块 |
| AVL-013 Schedule 完整编辑页 | ⚠️ | 编辑器在 StudentProfilePage:408-414；StudentSchedulePage 无学习日规则入口（§11.1 职责 6 不符） |
| AVL-014 保存立即生效 | ✅ | 保存后失效 schedule 查询（WeeklyPatternEditor.tsx:61-68；WeekPlanEditor.tsx:169-176）；顺延实时重读 calendar（adapter :1289-1293） |
| AVL-015/016/017 必测 | ✅ | taskTransitions.test.ts:65-78（周四关→周五）、:80-91（override=false 跳过）、:93-110（默认关 override=true） |

### 4.2 DLY-001~022（Carry Forward）

| 编号 | 状态 | 证据 |
|---|---|---|
| DLY-001 provenance 摘录 | ⚠️ | provenance.md:37 登记源路径+SHA-256+EXTRACT 决策；未摘录 delayFollowingLesson 原实现 |
| DLY-002/003/004 纯 TS 逐日扫描 | ✅ | availability.ts:83-107；无 gap=1 week、无 class conflict |
| DLY-005/006 override 优先→回落默认周 | ✅ | availability.ts:53-61,63-73；测试 taskTransitions.test.ts:48-63 |
| DLY-007 availableMinutes>0 | ⚠️ | 文档标注"可选"；只查 available（availability.ts:100），UI 层提示填写；未做该检查 |
| DLY-008 设备冲突继续向后 | ✅ | availability.ts:101-103 |
| DLY-009 90 天返回 null | ✅ | availability.ts:90,94,106；null→源置 BLOCKED（taskTransitions.ts:203-208；adapter :1317-1323） |
| DLY-010 循环非递归 | ✅ | availability.ts:94 for 循环 |
| DLY-011/012/013 clone 当前任务快照+新 id+lineage | ✅ | taskTransitions.ts:231-242 {...source}；adapter :1334-1372 全快照列、:1295 新 uuid、:1340,1365,1375-1386 双向回填 |
| DLY-014 旧任务不删除 | ✅ | :1326-1331 仅 UPDATE；deleteTask 拒删历史（:2027-2029） |
| DLY-015 Track pointer 不变 | ✅ | 顺延事务（:1316-1389）无任何 track 表语句 |
| DLY-016 locked no-op | ✅ | taskTransitions.ts:192-194 changed:false；adapter :1306-1314 reason NO_OP；测试 TaskContextMenu.test.tsx:29-37 |
| DLY-017 非 PENDING 拒绝 | ✅ | taskTransitions.ts:192；DayClose SQL 只取 PENDING（adapter :1411） |
| DLY-018 幂等：无两个有效 target | ⚠️ | 主路径有保护（adapter :1284-1288 查 carried_from 且 status<>CANCELLED；taskTransitions.ts:222-231 复用 existing）；但 existing 匹配要求**同日期**才复用，理论可造出同源两个 PENDING target（见问题 M-4） |
| DLY-019 preview 不写库 | ❌ | 全库无 previewCarryForward 实现 |
| DLY-020 DayClose 复用同一函数 | ✅ | adapter :1453 循环内直接调 this.carryForwardTask；测试 :411-507 |
| DLY-021 日结显示顺延 X 项 | ✅ | TodayPage.tsx:371-394,482-488 |
| DLY-022 顺延标签 tooltip 来源日期 | ⚠️ | TaskCard.tsx:274 仅 Tag"顺延"，无 tooltip/来源日期；Today 明细表可查（:770-774）但 TaskCard 本体未满足最低要求 |

### 4.3 DLY-T01~T10 必测场景

| 场景 | 状态 | 说明 |
|---|---|---|
| T01 周四不可学→周五 | ✅ | taskTransitions.test.ts:65-78 |
| T02 周五→跳周末→下周一 | ❌ | **无用例**（既有用例扫描窗口均不经过周六/周日） |
| T03 override=false 跳过 | ✅ | :80-91 |
| T04 默认关 override=true | ✅ | :93-110 |
| T05 locked→不移动 | ⚠️ | 无 domain 单测；仅 DayClose SQL 过滤间接成立（adapter.test:439-442,466-472） |
| T06 已完成→不移动 | ⚠️ | 无直接用例；仅 SQL status='PENDING' 过滤 |
| T07 重复顺延不建第二目标 | ✅ | taskTransitions.test.ts:160-175 + adapter.test:501-506 |
| T08 顺延后 currentOrdinal 不变 | ❌ | **无用例**断言 track 表未被写（代码层成立） |
| T09 完成顺延新任务→Track 前进 | ⚠️ | complete 推进有测试（:112-120）但未用顺延 target 串联验证 |
| T10 历史实例仍可查询 | ✅ | adapter.test:483-499 lineage JOIN + getTodayCarryovers |

**F5 门禁结论：10 个必测场景仅 5 个有直接自动化用例，"DLY-T01~T10 通过"未实质满足。**

### 4.4 INT-CAL-001~012 与 RSC-001~008

| 编号 | 状态 | 证据 |
|---|---|---|
| INT-CAL-001 | ⚠️ | 月视图已换 Flowclass MonthView（StudentSchedulePage.tsx:875-956；测试 :203-258）；Day/Week 仍自建 DayCard/DayCell（:1015-1305） |
| INT-CAL-002 | 🚫N.A. | §19.9 明确 superseded |
| INT-CAL-003 | ⚠️ | 右键菜单完整（TaskCard.tsx:126-155 → TaskContextMenu.tsx:86-145）；左键单击/详情为占位 message.info（StudentSchedulePage.tsx:760,850,946） |
| INT-CAL-004 | ✅ | :170-172 → taskActions.complete → adapter completeTask |
| INT-CAL-005 | ✅ | :567-603 handleDragEnd → rescheduleMutation → taskActions.reschedule |
| INT-CAL-006 | ✅ | 学习日判定在 domain（adapter :1851-1864 调 transitionRescheduleTask；非学习日记 overrideReason）；注意与 RSC-006 原文的偏离见 MINOR-9 |
| INT-CAL-007 | ✅ | useDraggable disabled（:1342-1345）+ canMoveCalendarEvent（:574-582）+ domain 兜底（taskTransitions.ts:142-144）；测试 :297-334 |
| INT-CAL-008 | ✅ | TaskCard.tsx:116-120,193,196-201 history→disabled+opacity；测试 :260-295 |
| INT-CAL-009 | ⚠️ | 排序仅 sortOrder/createdAt（adapter :2703；TodayPage.tsx:78-85）；priority/star 不参与排序（见 M-3） |
| INT-CAL-010 | ✅ | MAX_TASKS_PER_CELL=10 + "+N more"（:66,1296-1300） |
| INT-CAL-011 | ✅ | taskTransitions.ts:166-176 不触 status/ordinal；SQL 不 SET status；测试 :191-205,230-238 |
| INT-CAL-012 | ✅ | 全仓唯一写 current_ordinal 的路径：complete/reopen 回退/delete 回滚；reschedule/carryForward 均不写 track |
| RSC-001 | ✅ | 四入口（Calendar 拖拽/矩阵拖拽/RescheduleModal/TaskCard 右键）收敛于 taskActions.reschedule（taskActions.ts:11-17）→ scheduleApi.rescheduleTask（:67-83）→ domain |
| RSC-002 | ✅ | scheduled_date 写入仅 rescheduleTask SQL（adapter :1869）与 mount/scheduleTrackItems 初始化（:2578）；无页面旁路 |
| RSC-003/004/005 | ✅ | locked 拒绝（taskTransitions.ts:142-144 + SQL AND locked=0 :1875 + 测试 :207-215）；不改 status/ordinal（:166-176；测试 :191-205,230-238） |
| RSC-006 | ⚠️ | 已按 2026-08-20 用户反馈改为"照做+记录 overrideReason"（taskTransitions.ts:126-133,155-164；测试 :217-228）；与任务书原文"弹 warning"有记载地偏离，文档未同步 |
| RSC-007 | ✅ | MANUAL+manualOverride+overrideReason（:172-174）vs CARRYOVER（adapter :1343）可区分 |
| RSC-008 | ✅ | 同 SQL 同 domain；taskActions.test.ts:6-43 验证四视图失效键统一 |

### 4.5 测试套件规模

14 个测试文件 / 61 个用例（58 个 it + it.each 展开 3 例）——与 §19.9 一致，与主代理 2026-08-27 实测一致。

### 4.6 审计线 C 问题清单

**CRITICAL**

1. **previewCarryForward 缺失（DLY-019/§9.3/§18.4 承诺）**：§9.3 四核心服务只落地三个；无"先看预计落点再确认"能力，且没有现成纯函数可复用。建议在 taskTransitions.ts 增加纯函数（内部复用 findNextAvailableStudyDate，无 BLOCKED 副作用），carryForwardTask 改为 preview+写库两段。
2. **DLY-T02/T08 缺失、T05/T06 仅间接成立**：跨周末路径与"顺延对 Track 零副作用"两个核心不变量无回归保护。建议补 4 个 domain 用例（T02 周五→下周一、T05 locked→changed:false、T06 COMPLETED→changed:false、T08 断言 currentOrdinal 恒等）。

**MAJOR**

3. **同日排序缺 priority/star（INT-CAL-009）**：adapter :2703 与 TodayPage :78-85 只按 sortOrder/createdAt。建议补 star DESC/priority 权重，或在文档收口为"sortOrder/createdAt+视觉标记"。
4. **DLY-018 幂等缺口**：existing target 匹配要求同日期（taskTransitions.ts:222-231）+ adapter 只取 LIMIT 1（:1284-1288）；target 被改期后源异常复位 PENDING 的理论场景会生成第二个 PENDING target。建议放宽匹配为"同学生/track/ordinal 且 PENDING"，或加 UNIQUE(carried_from_instance_id) WHERE status='PENDING' 部分索引兜底。
5. **任务详情占位（INT-CAL-003/DLY-022）**：五处"详情待实现"（StudentSchedulePage:760,850,946、StudentWorkbenchPage:607、TodayPage:645）；TaskCard 顺延 Tag 无来源日期 tooltip。建议 P1 最小实现 tooltip；详情页另立任务。
6. **Schedule 页缺学习日规则入口（AVL-013/§11.1 职责 6）**：建议页面头部加"学习日设置"跳转。

**MINOR**

7. DLY-001 溯源未达"摘录原实现"标准（provenance.md:37 仅哈希+决策）。
8. AVL-006 交互形态偏离（整周替换式）若为有意简化应补记录。
9. RSC-006 改期直生效与"expectedVersion 严格校验仅约束 deleteTask"（adapter :1709-1716 注释、:2024,2078-2080）两处文档-代码口径漂移需补记。
10. task_instance 的 zod 视图 schema 四处重复无单一来源（taskApi.ts:5-38、today/taskApi.ts:4-32 且字段口径不一致、scheduleApi.ts:4-33、workbenchApi.ts）；新增列易漏改。建议抽 taskViewSchema 基座。
11. AVL-011 "复制上周"被空态门禁（WeekPlanEditor.tsx:83-136）；建议工具栏补"重置为上周"。

---

## 5. 审计线 F：模型边界与 WD 模块保留（主代理接手完成）

> 说明：该审计线原由子代理承担，因底层模型配额连续 402 中断六次未交付，由主代理以本地 grep/Read 接手完成；以下为全部取证结论。

### 5.1 DROP 合规（§1.2 / MODEL-DROP-001~005）

| 检查 | 判定 | 证据 |
|---|---|---|
| SaaS 概念源码残留 | ✅ 零命中 | ClassLesson/StudentLesson/EnrollCourse/CourseSelector/Invoice/Institution/WhatsApp/useAuth/Recoil/react-i18next/Radix/refresh_token 在 apps/web、apps/desktop 无任何命中 |
| organization/tenant/attendance/Course | ✅ 仅良性命中 | 全部位于 provenance.md 来源记录（:30,54-55）、vendor 文件头改造注释（StudentTaskMatrixShell.tsx:4,11,30；MonthView.tsx:10）、无残留守卫测试（StudentSchedulePage.test.tsx:139-166） |
| MODEL-DROP-001~005 无 SaaS 表 | ✅ | migration 恰 13 张表：student、student_weekly_pattern、student_weekly_pattern_day、student_date_override、task_template、task_template_version、task_template_item、student_task_track、task_instance、vocabulary_batch、vocabulary_entry、app_setting、idempotency_record；无任何 Class/ClassLesson/StudentLesson/EnrollCourse/organization 表 |
| 可用性数据无课程外键 | ✅ | weekly pattern/date override 独立建表，无课程关联 |

### 5.2 最终模型（§10.1/§10.3）

✅ 分层成立：task_template → task_template_version → task_template_item（UNIQUE(version, ordinal) migration:116）→ student_task_track（start/end ordinal + current_ordinal）→ task_instance。task_instance 字段齐全：track_id（:152）、template_version_id（:153）、item_ordinal（:155）、locked（:167）、carried_from_instance_id/carried_to_instance_id（:169-170）、sort_order/star（:176-177）；CHECK 约束保证 track 任务必带 item（:183）；部分唯一索引 uq_task_carry_target 防同源双目标（:192-194）。无双层实例，TaskInstance 是唯一执行真值。

### 5.3 WD 十模块保留度（§4.2）

✅ 全部存活：App.tsx:15-38 路由齐全（/today、/students、/workbench、/templates、/templates/:id、/imports + 学生三入口 /students/:id/{profile,vocabulary,schedule}）；API facade 全部经 getDataAdapter() 走本地 adapter（todayApi.ts:2 等），无 HTTP；AppShell 挂 GlobalSearchDialog（AppShell.tsx:5,71）；apps/desktop 提供 SQLite migration 宿主。

### 5.4 历史 PRD 能力存活抽查

- Excel 导入：features/importexport/ImportPage.tsx 存在且挂路由（App.tsx:35）；后端解析器现状待审计线 E 复核（LOC-023）。
- 全局搜索：GlobalSearchDialog 挂 AppShell.tsx:71。
- 学生三入口：App.tsx:18-29 独立路由。

### 5.5 风险清单 R1~R9 快判

| 风险 | 判定 |
|---|---|
| R1 SaaS 模型回潮 | 未触发（5.1 零命中） |
| R2 Calendar 虚构时刻 | 未触发（月视图按日期；Day/Week 自建无小时网格——审计线 C 旁证） |
| R3 矩阵覆盖虚拟化 | 未触发（useVirtualizer 保留，审计线 C 旁证 MAT-013） |
| R4 顺延删原任务 | 未触发（源置 CARRIED_OVER，deleteTask 拒删历史） |
| R5 clone last | 未触发（clone 当前任务快照，adapter :1334-1372） |
| R6 改期推进 Track | 未触发（reschedule SQL 无 track 语句，测试旁证） |
| R7 过早删 Java 无 parity | 部分触发：parity 契约成文但双侧运行证据未归档（审计线 A MINOR-2）；既成事实，需记录而非回滚 |
| R8 全局 CSS 污染 | 未触发（compat css scoped；AntD 页面无污染报告） |
| R9 domain 写进组件 | 基本未触发（complete/reschedule/carry/findNextDate 收敛于 domain/taskTransitions.ts + domain/scheduling/availability.ts；页面仅调 taskActions facade） |

### 5.6 DoD 12 条快判

1. Calendar 真实 PoC——✅（CalendarAdoptionDecision.md 有 PoC gate 记录）
2. Calendar 不要求课时/教师/教室/班级——✅（守卫测试 + 类型纯任务语义）
3. 矩阵优势进入且 WD 虚拟化/TaskCard/密度保留——✅（MAT 旁证）
4. Availability 交互进入且改为学习日/分钟模型——✅（AVL 主体成立，交互形态偏离见 C-8）
5. delay 模式吸收且错误语义未进入——✅（R4/R5 未触发）
6. 唯一 TaskInstance 真值——✅（5.2）
7. 只有完成推进 Track——✅（INT-CAL-012）
8. Today/Matrix/Calendar 操作一致——✅（ACC-070~074 + taskActions 单一失效键）
9. 桌面程序无登录/机构/Spring/PostgreSQL/远端依赖——✅ 代码层（ACC-001 的 exe 人工复核仍 MANUAL）
10. Spring 退役前 parity tests——⚠️（契约在、双侧运行证据未归档，A MINOR-2）
11. 源码来源索引完整——✅（provenance.md 含 SHA-256 与逐文件决策；DLY-001 摘录粒度见 C MINOR-7）
12. 勾/拖/写/挂/顺延脱离 Excel——✅ 代码层闭环（真机手感 MANUAL）

### 5.7 审计线 F 问题清单

- **MINOR-F1** previewCarryForward 未实现（与 C-CRITICAL-1 同项，不重复计数）。
- **MINOR-F2** execution-parity.md 未标注 supersession 范围（与 A MINOR-1 同项）。
- 总体：DROP 合规与模型收敛质量高，无新增 MAJOR 以上问题。

---

## 6. 审计线 E：本地 SQLite 数据层 + 在线架构清理

> 子代理续跑后完整交付；基线 04 §13/§14/§19.1/§19.9/§20-R10，参考 ADR-002。

### 6.1 §13.3 目标表（0001_local_core.sql）

✅ 全部齐备：student(:3)、student_weekly_pattern(:23，active 唯一部分索引 :34-36)、student_weekly_pattern_day(:38)、student_date_override(:48)、task_template(:67)、task_template_version(:84)、task_template_item(:102)、student_task_track(:120)、task_instance(:148)、vocabulary_batch(:196)、vocabulary_entry(:207)、app_setting(:225)、idempotency_record(:231)——共 13 张业务表，与 §19.9 声称一致。可选表 task_history/import_job 未迁移属记录在案的产品选择（ADR-002 #1：import job 仅存会话内存，adapter :275）。在线十类表（organization/user_account/user_role_assignment/identity_session/refresh token/tenant 等）确认不存在。

### 6.2 LOC-001~024

| 编号 | 判定 | 证据（sqliteLocalDataAdapter.ts 除注明外） |
|---|---|---|
| LOC-001 DataAdapter 接口 | ✅ | data/DataAdapter.ts:1-100；六仓职责收敛在单一 adapter（有意简化，见 MINOR-6 备注） |
| LOC-002 SQLite 接入锁定 | ✅ | tauri-plugin-sql（Cargo.toml:26）+ 自定义 Rust 事务命令（sqlx =0.8.6，local_database.rs:42-50）；dependency-lock.md:20-23 精确锁版 |
| LOC-003 migrations | ✅ | 单文件 0001_local_core.sql；lib.rs:9-14 注册；浏览器侧 ?raw 复用同一 SQL（browserSqliteStorage.ts:3,30） |
| LOC-004 UUID 保留 text | ✅ | 主键 TEXT；crypto.randomUUID（:500,619,786-787） |
| LOC-005/006 organization_id/created_by 移除 | ✅ | migration 无该列，grep 零命中 |
| LOC-007~012 六类 repository | ✅ | 学生 :455-571；模板 :765-993；Track :1034-1143；TaskInstance :1245-2114；可用性 :573-763；生词 :2116-2278 |
| LOC-013/014/015 Today/Workbench/Schedule | ✅ | getToday :1581-1620（按生分组）；getWorkbench :1622-1665；getSchedule :1667-1706 + scheduleWindow day/week/month（:151-169） |
| LOC-016 Global Search | ✅ | :2280-2356 四类 LIKE 查询，无 FTS5；辅助索引（migration:21,187,222-223）；ADR-002 #3 记录取舍 |
| LOC-017/018/021 complete/reopen/carryForward parity | ✅ | domain 纯函数 taskTransitions.ts:43-81,83-120,179-253；adapter 编排 :1718-1797,:1799-1843,:1281-1402 |
| LOC-019/020 mountTrack/scheduleTrackItems | ✅ | :1051-1143（幂等键+首实例+事务）；:1145-1243（从 current_ordinal 连续、复用既有 PENDING） |
| LOC-022 DayClose | ✅ | triggerDayClose :1404-1511；首页直接日结（TodayPage.tsx:42,371-372,484-485） |
| LOC-023 Excel import | ✅ | SheetJS xlsx 前端解析（:20,279-342）；executeTemplateImport 落 SQLite（:344-443）；ImportPage beforeUpload 截流不上传（:91-100） |
| LOC-024 adapter 切换 | ✅ | data/runtime.ts:6-23：__TAURI_INTERNALS__ → Tauri，否则 Browser（sql.js 内存+同一 migration）；VITE_DATA_MODE 被无视（runtime.test.ts:21-30 反证） |

### 6.3 R10 事务完整性（重点）

✅ **无中间态风险，三层防护**：

- **carryForward 三写同事务**：UPDATE 源→CARRIED_OVER（:1326-1332）+ INSERT 目标含 carried_from（:1334-1374）+ UPDATE 源 carried_to（:1375-1387）在同一 storage.transaction（:1389）；Rust 侧 pool.begin()…commit()（local_database.rs:56-88）；浏览器 BEGIN IMMEDIATE/COMMIT/ROLLBACK（browserSqliteStorage.ts:81-102）；Rust 回滚单测（local_database.rs:99-147）；lineage 双向断言（adapter.test:296-314）。
- **completeTask + Track 推进同事务**：UPDATE 任务（:1737-1744）+ UPDATE track（:1746-1763）+ 可选下一项 INSERT（:1764-1787）+ 幂等记录同一次 transaction（:1792）。
- **三层防中间态**：每条语句带 expectedRowsAffected（任一不满足整批回滚，local_database.rs:77-84）+ 乐观锁 AND version=$n 保留在语句内 + uq_task_carry_target 唯一索引兜底（migration:192-194）。
- **撤销顺延原子**：源↔PENDING、目标→CANCELLED、幂等记录同一事务（:1561-1577）。

### 6.4 删除契约（§19.9）

✅ 三条全部落实且有测试：expectedVersion 严格校验（deleteTask 强制 requiredNumber，:2024；DELETE...AND version=$2 + expectedRowsAffected=1，:2075-2081；陈旧版本拒删测试 :640-667）；CARRIED_OVER/CANCELLED 拒绝物理删除（:2026-2029 TASK_NOT_DELETABLE；测试 :615-637；UI 历史行禁删 TaskCard.tsx:119-129）；PENDING 顺延目标删除清理双向 lineage 外键（清源 :2036-2043、清目标 :2044-2051、Track 指针回退 :2055-2073；测试 :509-638）。

### 6.5 CLEAN-001~014

✅ 14 条全部可验证落地：AppShell 无 useAuth/ADMIN_ROLES（AppShell.tsx 静态导航 :7-12）；features/auth、AuthProvider/ContextGate 链已删（main.tsx:26-49 提供者链干净）；useBusinessDate 本机日历日（useBusinessDate.ts:1-4）；无 Vite proxy（vite.config.ts:8-12）；CSP connect-src 'self'（tauri.conf.json:23）；根 dev/build/check 全部无 Java/PostgreSQL（package.json:10-21）；apps/api、infra 目录不存在；README 本地单机（README.md:1-10）；check-local-runtime 黑名单防回归（:8-15）。

### 6.6 local-runtime 门禁与 ACC-001~005

- 门禁覆盖静态 import + 字面量动态 import() + fetch( 扫描（check-local-runtime.mjs:6-57）；当前 src 内 fetch/XHR/WebSocket/axios 全部零命中。理论绕过面见 MINOR-1。
- ACC-001~005 代码层全部 ✅（sqlite:assistant.db → app_config_dir，即 %APPDATA%/com.wonderedu.assistant/；migration 只跑一次 + WAL；关库重开测试 :1057-1088）；exe 实机维度按验收矩阵仍 MANUAL。
- dependency-lock.md 与现状一致：无 Spring/PostgreSQL/Java/POI/Flyway 条目；tauri-plugin-sql 2.4.0 / SQLx 0.8.6 / sql.js 1.13.0 / xlsx 0.18.5 与 Cargo.toml、package.json 精确对上。

### 6.7 审计线 E 问题清单（无 CRITICAL/MAJOR，6 MINOR）

1. **local-runtime 门禁理论绕过面**：计算型动态 import、fetch 值别名（`const g=fetch`）、XHR/WebSocket/EventSource、bare-specifier（axios 等）均不命中（check-local-runtime.mjs:16-17,19-34,50-52）。当前无任何绕过且 CSP 兜底；建议扫描值引用与 XHR 族、对 bare-specifier 建允许清单。
2. **桌面 smoke 表数阈值过宽**：verify-desktop.ps1:30 `tableCount < 10` 才失败，误删 1-2 张表不会被抓；建议改 `!== 13` 或按表名逐一核对。
3. **browserSqliteStorage.ts:34 注释残留 VITE_DATA_MODE 说法**（该变量已被 runtime.ts 无视且有反证测试）；建议改注释。
4. **Cargo.toml:4 描述残留在线措辞**（"业务规则由助教工作台 API 提供"）；建议改为本地运行时表述。
5. **carryForward 理论脏状态分支**：existing 命中且无需变更时 statements 为空直接返回 CARRIED_OVER（:1306-1314,1389），若源行异常仍 PENDING 则响应与库不符——正常路径不可达（uq 索引+事务回滚兜底），属防御性缺口；建议空分支重读源行状态为准。
6. **观察项（不计缺陷）**：六 repository 收敛为单一 adapter 属允许的结构简化；app_setting 已建表但暂无读写（业务日期取本机时钟）；LIKE '%…%' 前置通配符不走索引，与 ADR-002 #3 取舍一致。

---

## 7. 审计线 B：Calendar 移植 + 样式兼容层 + 来源追踪

> 子代理对捐赠源 F:\workspace\flowclass-main 做了现场 sha256 校验，与 provenance.md 逐字吻合。

### 7.1 Calendar 移植（FCL-CAL-001~031）

**总体：Partial adoption 执行质量高、决策记录诚实。** 两个 ADAPT 文件（MonthView、StudentTaskMatrixShell）的来源哈希经现场实测与 provenance.md:29-30 完全一致；Recoil/i18n/Radix/课程域词汇/新依赖五条红线全仓 grep 零命中。

| 编号 | 状态 | 证据 |
|---|---|---|
| FCL-CAL-001/002/003/006/011/012/014 | 🚫N.A. | PoC 后 DROP 或被 Partial 决策取代，均有记录（provenance.md:31-35；CalendarAdoptionDecision.md:22-31）；去 Recoil/sample-data 意图以更强方式达成（grep 零命中，页面 useState 持有状态） |
| FCL-CAL-004/005/007/008/009/010/013/015 | 🚫/⚠️ | 未移植；除 CalendarHeader（⚠️：意图达成但缺"上一周期/下一周期"按钮，仅原生 date input，属产品取舍）外均未逐条进弃用记录（见 B-MINOR-1） |
| FCL-CAL-016 MonthView PoC→产线 | ✅ | vendor/flowclass/calendar/MonthView.tsx:37-58 接 StudentSchedulePage.tsx:876；测试 calendar.test.tsx:11-82 + StudentSchedulePage.test.tsx:204-258 |
| FCL-CAL-017/018 Week/Day PoC | ✅ | PoC 后保留 WD 自有布局（无小时语义、风险 R2），CalendarAdoptionDecision.md:40-45 |
| FCL-CAL-019 DECISION | ✅ | Partial 决策+保留清单+弃用表+INT-CAL-002 superseded（:3,9-38）；弃用表覆盖约七成（B-MINOR-1） |
| FCL-CAL-020~027 DROP 项 | ✅ | QuotaAttendance/AttendanceIndicatorBar/CalendarSidebar/CreateClassFromCalendarModal/LessonDetail*/QrCodeView/Google Calendar 全仓 grep 零命中 |
| FCL-CAL-028/029 taskCalendarAdapter | 🚫N.A. | §19.9 superseded（04:1421）；结构契约由 canMoveCalendarEvent 承担（types.ts:24-35） |
| FCL-CAL-030 纯任务类型 | ✅ | types.ts:1-17 仅任务语义字段 |
| FCL-CAL-031 拖拽回调 taskId+targetDate | ✅ | StudentSchedulePage.tsx:589-594；version 为 domain 乐观锁必需；跨学生 targetStudentId 为 §19.9 已记录的产品调整 |

### 7.2 样式兼容层（STYLE-001~010）

| 项 | 状态 | 说明 |
|---|---|---|
| STYLE-001/002/004/005 Tailwind | ❌有意偏差 | 无 tailwind/postcss 依赖与配置；实现走手写 scoped CSS。字面不符、意图（R8 防错）完全达成，但未形成显式决策记录（B-MINOR-2） |
| STYLE-003/008 无 preflight/无全局规则 | ✅ | compat.css 全部规则 scope 在 .flowclass-scope/.flowclass-matrix-* 下（:2-142），无任何全局选择器 |
| STYLE-007 box-row-full | 🚫N.A. | Availability 走重写路线，条件未触发 |
| STYLE-009 第三方包记录 | ⚠️ | provenance 无"已引入第三方包"小节（实际零新增，见 B-MINOR-3） |
| STYLE-010 lint/typecheck 覆盖 | ✅ | tsconfig.app.json:24 include src 覆盖 vendor；eslint 无 vendor ignore（执行通过为文档声称，本次未复跑——主代理 2026-08-27 已实测复跑通过） |

**依赖最小化 ✅（比任务书更严格）**：§6.2 允许的 react-dnd/dayjs 一个都没引入（复用已有 @dnd-kit + 原生日期；RescheduleModal.tsx:34 注释明示）；禁入清单（recoil/react-query v3/Flow i18n/Radix）全数零命中；lockfile 中 dayjs 仅为 antd 传递依赖。

### 7.3 来源追踪（SRC-001~005）与 ACC-056

- SRC-001~005 主体 ✅：provenance.md 含快照身份/逐文件 SHA-256/ADAPT×2+DROP×5+REFERENCE+EXTRACT 决策/重同步三步流程/MIT 声明；"无未解释文件"承诺经盘点属实（8 个 vendor 文件：2 个带完整六要素来源注释，6 个 WD 自写已在"WD-authored"表登记）。
- ACC-056 ✅：守卫测试（StudentSchedulePage.test.tsx:138-175）逐视图封禁课程/班级/教师/教室/考勤等 11 词，中英文双语言。

### 7.4 审计线 B 问题清单（无 CRITICAL）

- **MAJOR-1** 迁移成果全部未纳入版本控制：apps/web/src/vendor/ 与 docs/migration/ 整目录 untracked，HEAD 停在 3b93f57，任何分支上都不存在这些文件的历史——一次 git clean 即丢失且 SHA-256 链条随之失效（与审计线 A MINOR-4 同源，此处升级为该线内 MAJOR）。建议按 §21 提交粒度提交（提交策略由用户决定，§19.9:1426 已声明）。
- **MINOR-1** 弃用文件清单记录不完整（SRC-002/003 字面缺口）：CalendarGrid/各 Dropzone/WeekViewEvents/EventItemWithError/ColumnViewWrapper/CalendarHeader/EventPopover 及 CreateClassFromCalendarModal/LessonDetail*/QrCodeView 未逐条进弃用记录；建议补一行"§5.1A/B 其余文件：随事件层整体 DROP"。
- **MINOR-2** STYLE-001~005"放弃 Tailwind"未形成显式决策记录；建议补"手写 scoped CSS 等价实现，STYLE-001/002/004/005 superseded"。
- **MINOR-3** provenance.md 缺"已引入第三方包"小节；建议补"第三方依赖：无新增；dayjs 仅为 antd 传递依赖"。
- **MINOR-4** flowclass-compat.css 被双重导入（main.tsx:8 全局 + StudentSchedulePage.tsx:57 页面级）；建议保留一处。
- **MINOR-5** TaskCalendarEvent/MoveCalendarEventInput 为无消费方的死类型（types.ts:6-22，INT-CAL-002 superseded 残留）；有被接回"第二层事件模型"的误导风险（DoD #6 明令禁止）；建议加注释或删除。

---

## 8. 汇总结论

1. **acceptance.md 的 46 项覆盖与代码高度相符，AUTO 声称诚实，无虚假验收**；核心闭环均有代码+测试双证据；实时门禁（pnpm check 61 用例、format、local-runtime 守卫、cargo）2026-08-27 复测全绿。
2. **真正的缺口在门禁记录层与测试覆盖**，不在产品代码：F0~F5 缺逐项收口、F0-004 视觉基线永久缺失、F9 MANUAL 复核无执行记录、根目录 planning 三件套整体漂移（最高优先处理）、DLY 必测场景 5/10 缺直接用例。
3. **调度域实现质量高、收敛度高**（单一 domain service + 单一 DataAdapter + 四视图统一失效），遗留缺口为 previewCarryForward、排序维度、详情占位与若干文档口径漂移。
4. **DROP 合规完全干净**，无 SaaS 模型回潮；§10 最终模型与 §24 DoD 在代码层全部成立（除 parity 双侧证据与真机 MANUAL 项）。
5. **数据层是六条线中质量最高的一条**：LOC/CLEAN 全部可验证落地，R10 事务有三层防中间态（expectedRowsAffected + 乐观锁 + uq 唯一索引），删除契约与测试一一对应，无 CRITICAL/MAJOR，仅 6 条 MINOR（门禁加固 2 条 + 文档/注释残留 3 处 + 1 个理论脏状态分支）。
6. **Calendar/Matrix 移植是执行质量很高、决策记录诚实的 Partial adoption**：两个 ADAPT 文件的来源哈希经对捐赠源现场实测与 provenance 完全吻合，Recoil/i18n/Radix/课程域词汇/新依赖五条红线全仓零命中；比任务书更严格（允许的 react-dnd/dayjs 也未引入）。遗留为文档完整性级小缺口（弃用表覆盖约七成、STYLE 偏差未记、死类型残留）。

## 9. 待补审计线

- ~~审计线 D（矩阵融合与工作台）——子代理运行中（最后一个）~~ **已回（2026-08-28，历经多次网关中断后完整交付），六线闭环。结论见 §10。**

（审计线 F 的全新复核子代理因底层模型配额连续 402 未交付，F 线以主代理接手版为准。）

---

## 10. 审计线 D：矩阵融合与工作台（2026-08-28 增补）

> 取证基于实际安装包源码（antd 6.6.0/@rc-component/table 1.11.1、@dnd-kit/core 6.3.1、react-dom 19.2.8），非文档推断。MAT-001~025：21 项 ✅、4 项 ⚠️（MAT-003/024/025 及门禁滚动项）；DROP 清理、来源追踪、60×14×3 虚拟化证据扎实；跨学生拖拽三层防线完整（workbenchDrag.ts:36-43 三拒 + reschedule 跨学生 SQL 脱离 Track + domain 兜底）。

### 10.1 两个 jsdom 盲区缺陷（本线核心发现）

- **D-1 CRITICAL：DndContext 漏配 sensors，卡片内左键点击被吞没。** StudentWorkbenchPage.tsx:699 的 DndContext 未传 sensors，dnd-kit 默认 PointerSensor 无 activationConstraint（core.esm.js:2468-2473），指针按下即激活（:1469-1471）并在 document 捕获阶段 stopPropagation 吞掉 click（:1489-1495；detach 延迟 50ms，:1475-1477）。净效果：矩阵内 TaskCard 的 checkbox 完成/重开、优先级、悬浮删除、卡内 React 链接全部无反应；右键菜单/拖拽/空 cell 按钮/Today/Schedule 页不受影响。MATRIX-GATE"Task checkbox 可用"实际不成立，acceptance 的 AUTO 判定对此项失真。修复一行式：照抄 StudentSchedulePage.tsx:553 的 distance:6 约束。
- **D-2 MAJOR：components.body 忽略 rc-table 第二参数，表头/表体横向滚动脱钩。** shell :97 的 scroll={{x,y}} 触发分体表头，但自定义 body 签名 `(rows) => …` 丢弃了 `{ ref: scrollBodyRef, onScroll: onInternalScroll }` 同步接线（Table.js:482-484），body 在 .flowclass-matrix-viewport 自行横滚而表头停在 scrollLeft=0；表头 wheel 捕获还会单侧滚动（FixedHolder/index.js:59-90）。"第一列固定"退化为仅表体成立。

### 10.2 其余问题（矩阵线独有）

- D-3 MINOR：MAT-003"7/14 天"仅实现 7 天、无双周入口（与 WD 原逻辑一致，无回归，验收口径待收口：标 superseded 或补双周）。
- D-4 MINOR：空 cell 加任务路径无 workbench 级用例（InlineTaskComposer 在 Today 有覆盖）。
- D-5 MINOR：MAT-025 密度切换保持性无显式断言（仅结构推理）。

### 10.3 行动清单增补

| # | 级别 | 事项 | 状态 |
|---|---|---|---|
| 13 | CRITICAL | D-1 点击吞没（sensors 一行修复 + userEvent 用例） | 修复代理已派（2026-08-28） |
| 14 | MAJOR | D-2 表头滚动脱钩（body 第二参数接线或单容器方案 + 滚动断言） | 同上 |
| 15 | MINOR | D-3/D-4/D-5 验收口径与测试补齐 | D-4/D-5 随修复代理；D-3 待产品决策 |
