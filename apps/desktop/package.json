{
  "name": "@assistant/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:no-bundle": "tauri build --no-bundle",
    "build:mac": "bash scripts/build-macos-dmg.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
